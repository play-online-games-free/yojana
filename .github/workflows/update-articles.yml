name: Update Article List
on:
  push:
    paths:
      - 'govt-jobs/*.html'

jobs:
  update-json:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Generate JSON
        run: |
          # Add a script here to scan the /govt-jobs/ directory,
          # extract titles/descriptions, and update govt-jobs.json
          # Example: Use Python/Node.js to automate this.

      - name: Commit and Push Changes
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          git add .
          git commit -m "Auto-update article list"
          git push
